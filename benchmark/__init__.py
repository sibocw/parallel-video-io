"""Benchmark suite comparing parallel-video-io (PVIO) against other video IO libraries.

Run ``python -m benchmark.run_all`` after ``uv sync --extra benchmark``.
See ``benchmark/README.md`` for details.
"""
